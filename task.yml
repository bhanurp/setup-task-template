name: setup-yarn
description: Setup yarn package manager
source: bhanurp/setup-yarn
labels:
  - yarn
  - setup
input:
  - name: version
    description: version to be installed
execution:
  - node16 dist/setup.js main